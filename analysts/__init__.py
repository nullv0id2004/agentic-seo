"""Reasoning plane. Every analyst reads rows the runtime hands it and returns strict JSON.

No network. No fetch. No shell. No database handle. The import lint enforces the first; the
AnalystContext enforces the rest by simply not offering them.
"""
